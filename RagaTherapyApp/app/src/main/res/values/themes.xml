<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.MusicApp" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- App Brand Colors -->
        <item name="colorPrimary">@color/color_primary_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Proper background color key -->
        <item name="colorSurface">@color/color_background</item>

        <!-- Set default window and system bar colors -->
        <item name="android:windowBackground">@color/color_background</item>
        <item name="android:statusBarColor">@color/color_primary_900</item>
        <item name="android:navigationBarColor">@color/color_primary_200</item>
    </style>

    <style name="Theme.MusicApp" parent="Base.Theme.MusicApp" />
</resources>
